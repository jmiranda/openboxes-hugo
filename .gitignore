# Hugo default output directory
/public

# Visual studio config
.vscode

# IDEA project files
.idea

# Project-local pinned Hugo binary (see CLAUDE.md)
/bin

# Hugo transient build lock
.hugo_build.lock

# Screenshot tooling (dev-only, for PR documentation — not part of the site)
/node_modules
/package.json
/package-lock.json
/screenshots
/tools